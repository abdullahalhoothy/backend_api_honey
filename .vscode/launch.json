{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI: Debug",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app_endpoints:app",
                "--reload",
                "--host", "localhost",
                "--port", "7000"
            ],
            "jinja": true,
            "env": {
                "DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/mydatabase"
            }
        }
    ]
}